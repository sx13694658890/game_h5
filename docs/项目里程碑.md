# 星际弹球（StarPinball）— 项目里程碑与任务进展

> **用途**：一眼区分「已完成 / 部分完成 / 未完成 / 未验证」，与 `docs/任务计划与需求清单.md` 清单编号对齐。  
> **更新方式**：实现或验收变动后，只改本文件对应行的「进展」列与文末「统计」。  
> **快照日期**：2026-04-15（阶段三实现后已更新本页 §一、§二 阶段三～四、§三、§五）  

---

## 图例（进展列统一用下列符号）

| 符号 | 含义 |
|:----:|------|
| ✅ | **已完成**：当前代码与行为已覆盖文档要求的主要验收点 |
| 🔶 | **部分完成**：有实现或占位，但未满足文档全部验收标准，或未接入主流程 |
| ⬜ | **未完成**：无实现或仅有空壳，无法验收 |
| ⚠️ | **未验证**：代码侧已具备能力，但尚未按文档做真机/专项测试与签字 |

---

## 一、总览（按监管五阶段）

**列含义**：✅ = 实现已达验收；🔶 = 做了但未达标或未接线；⬜ = 未做；⚠️ = 仅缺测试/签字（实现已有）。

| 阶段 | 主题 | 检查项数 | ✅ | 🔶 | ⬜ | ⚠️ | 一句话 |
|------|------|----------:|--:|--:|--:|--:|--------|
| 阶段一 | 项目初始化 | 7 | 6 | 1 | 0 | 0 | 工程脚手架齐；Git 需本机确认 |
| 阶段二 | 核心玩法 | 9 | 9 | 0 | 0 | 0 | 玩法逻辑已实现；真机 Review 见 §二表末行 ⚠️ |
| 阶段三 | 特效与体验 | 7 | 6 | 1 | 0 | 0 | 粒子/拖尾/音效/过渡/暂停/通关弹窗已接；星空无流星层 |
| 阶段四 | 性能与兼容 | 8 | 2 | 1 | 0 | 5 | gzip + `visibilitychange` 暂停已接；其余多 ⚠️ 待真机测 |
| 阶段五 | 上线前最终 | 6 | 5 | 1 | 0 | 0 | 榜/设置/弹窗/构建已接；多比例与部署 URL 仍 🔶/⚠️ |
| **合计** | — | **37** | **28** | **4** | **0** | **5** | 下方 §二 为细账；加总 = 37 |

**与需求清单表格的关系**：需求清单里「状态」列仍由负责人实测后打勾；本文件「进展」表示**开发完成度**，二者可并存（例如阶段二代码 ✅ 但清单仍可 ⬜ 直到真机签）。

---

## 二、监管 Review 清单（逐项进展）

与 `任务计划与需求清单.md` §一 表格逐条对应。

### 阶段一：项目初始化（7 项）

| # | 检查项 | 进展 | 说明（证据） |
|---|--------|:----:|--------------|
| 1 | Vite + React18 + TS 能启动 | ✅ | `package.json` + `pnpm run dev` / `build` |
| 2 | Tailwind 生效 | ✅ | `tailwind.config.js`、`src/index.css`、`App.tsx` 等 |
| 3 | ESLint + Prettier | ✅ | `eslint.config.js`、`.prettierrc`、`pnpm run lint` |
| 4 | 目录结构符合架构 §3.1 | ✅ | `src/pages`、`components/*`、`hooks`、`store`、`engine`、`config` 等 |
| 5 | Git + `.gitignore` | 🔶 | `.gitignore` 已含 `node_modules`；仓库是否在您环境 `git init` 请自行确认 |
| 6 | viewport meta | ✅ | `index.html`：`width=device-width, initial-scale=1` 等 |
| 7 | 全局 CSS 变量 | ✅ | `src/index.css` `:root` 颜色与字体变量 |

### 阶段二：核心玩法（9 项）

| # | 检查项 | 进展 | 说明（证据） |
|---|--------|:----:|--------------|
| 1 | Canvas 适配屏幕 | ✅ | `GameCanvas`：DPR、`ResizeObserver`、逻辑坐标与缩放 |
| 2 | 弹球匀速、无抖动 | ✅ | `enforceBallSpeed`、`useGameLoop` 限帧 dt |
| 3 | 墙壁入射角 = 反射角 | ✅ | `bounceWallX` / `bounceWallY` |
| 4 | 挡板触摸跟随 | ✅ | `usePaddleControl` + `setPointerCapture` |
| 5 | 挡板左/中/右偏转不同 | ✅ | `reflectOffPaddle` |
| 6 | 砖块碰撞、得分 | ✅ | `collision.ts`、`onBrickDestroyed`、`BRICK_SCORE` |
| 7 | 落底扣命、0 命结束 | ✅ | `registerBallLost`、`GamePage` 遮罩 |
| 8 | 连击得分加成 | ✅ | `brickStreak`、倍率上限 ×5 |
| 9 | 砖块清空 → 关卡完成 | ✅ | `notifyLevelClear` + `LevelClearModal` |
| **（追加）** | **真机逐项 Review 签字** | ⚠️ | 不在原 9 条内；建议在真机测完后于《任务计划与需求清单》打勾 |

### 阶段三：特效与体验（7 项）

| # | 检查项 | 进展 | 说明 |
|---|--------|:----:|------|
| 1 | 砖块消除粒子爆炸 | ✅ | `particleEngine` + `GameCanvas`：12～20 粒、重力、透明度衰减；金砖更亮更多 |
| 2 | 球尾拖尾 | ✅ | 最近 5 帧位置渐隐绘制 |
| 3 | 星空背景动画 | 🔶 | 随机星点 CSS 闪烁 + 渐变；**无**文档 Sprint2 的流星/多层视差 |
| 4 | 音效正常 | ✅ | `audioSynth` + `useGameAudio`，挡板/碰砖/消砖/失命/通关/结束；受 `settingsStore.soundEnabled` 控制 |
| 5 | 页面切换过渡 | ✅ | `App`：`AnimatePresence` + `framer-motion`（菜单 ↔ 游戏） |
| 6 | 暂停/恢复、状态保留 | ✅ | 「暂停」按钮、`pauseGame`/`resumeGame`、`PauseModal`；`playing` 才跑 RAF；切后台 `visibilitychange` 自动暂停 |
| 7 | 关卡完成弹窗（本关得分等） | ✅ | `LevelClearModal`：本关得分、累计分、1～3 星依次亮起、「下一关」 |

### 阶段四：性能与兼容性（8 项）

| # | 检查项 | 进展 | 说明 |
|---|--------|:----:|------|
| 1 | 稳定 60fps | ⚠️ | 未做 DevTools 专项记录 |
| 2 | iOS Safari | ⚠️ | 未测 |
| 3 | Android Chrome | ⚠️ | 未测 |
| 4 | 触摸与滚动不冲突 | 🔶 | `touch-action` / `touch-none` 有配置；未系统验收 |
| 5 | 后台自动暂停 | ✅ | `GamePage`：`document.hidden` 时 `playing` → `paused` |
| 6 | 内存无明显泄漏 | ⚠️ | 未测 |
| 7 | Bundle gzip &lt; 200KB | ✅ | 当前构建 JS gzip 量级约 50KB 档（仍建议每次发版复核） |
| 8 | 首屏 4G &lt; 2s | ⚠️ | 未测 |

### 阶段五：上线前最终（6 项）

| # | 检查项 | 进展 | 说明 |
|---|--------|:----:|------|
| 1 | 本地排行榜 Top10 | ✅ | `utils/leaderboard` + `LeaderboardPage`；游戏结束写榜；高亮本局；清空二次确认 |
| 2 | 设置页音效开关 + 持久化 | ✅ | `SettingsPage`；`settingsStore` 写 `localStorage`；难度影响球速/挡板（`GameCanvas`） |
| 3 | 所有弹窗可关闭、不卡死 | ✅ | 暂停：遮罩/✕ 继续；通关弹窗 ✕ 同「下一关」；无卡死回归 |
| 4 | 多屏幕比例 | 🔶 | `MobileFrame` 增加 `max-h-[min(100dvh,calc(100dvw*2.22))]` 限高；**未**做 16:9/19.5:9/20:9 真机矩阵签字 |
| 5 | 生产构建无 warning | ✅ | 当前 `pnpm run lint` / `pnpm run build` 无 ESLint error、构建无 warning |
| 6 | 部署 URL 可访问 | 🔶 | 已加 `vercel.json` 与 `public/_redirects`（SPA 回退）；实际上线 URL 仍依赖你方托管配置 |

---

## 三、功能需求 FR（逐项进展）

| ID | 进展 | 说明 |
|:--:|:----:|------|
| **FR-01** 启动加载页 | 🔶 | 存在 `LoadingPage` 占位；**未**接入 `App` 首屏；无 Logo 动画/进度条/5s 提示 |
| **FR-02** 主菜单 | 🔶 | 有 Logo、开始、**最高分**、排行榜/设置入口；仍缺新手引导等 |
| **FR-03-01** | ✅ | Canvas 内物理运动 |
| **FR-03-02** | ✅ | 指针控制挡板 |
| **FR-03-03** | ✅ | 挡板偏转反弹 |
| **FR-03-04** | ✅ | 砖块 HP、消除 |
| **FR-03-05** | ✅ | 10 / 20 / 50 分 |
| **FR-03-06** | ✅ | 连击倍率上限 ×5 |
| **FR-03-07** | ✅ | 落底扣命 + 球重置 |
| **FR-03-08** | ✅ | 0 命游戏结束 |
| **FR-03-09** | ✅ | 清空砖块 → `notifyLevelClear` + `LevelClearModal` |
| **FR-03-10** | ✅ | HUD：分、命、关、连击提示 |
| **FR-03-11** 暂停 | ✅ | 右上「暂停」+ `PauseModal`；`phase === 'paused'` 时停 RAF |
| **FR-04** 关卡系统 | 🔶 | 多关配置与切换有；**未满**文档「至少 5 关」；**无** P2 关卡持久化 |
| **FR-05** 粒子 | ✅ | Canvas 内散射、重力、渐隐；金砖加强 |
| **FR-06** 音效 | ✅ | Web Audio 合成；`useGameAudio` + `settingsStore.soundEnabled` |
| **FR-07** 暂停弹窗 | ✅ | 继续 / 重新开始 / 返回主菜单 |
| **FR-08** 关卡完成弹窗 | ✅ | 本关得分、累计分、星级依次动画、下一关；星级规则为**简化版**（非文档全部维度） |
| **FR-09** 游戏结束页 | 🔶 | `GameOverPanel`：得分、NEW RECORD、写榜、再玩/排行榜/回菜单；**非**独立路由页 |
| **FR-10** 排行榜 | 🔶 | Top10、清空确认、高亮；**缺**「当前局」与榜外同分并列等细规则 |
| **FR-11** 设置页 | ✅ | 音效开关 + 三档难度 + `localStorage` |

### NFR（非功能）速览

| ID | 进展 | 说明 |
|:--:|:----:|------|
| NFR-01～02、04、05～07 | ⚠️ | 未专项验证 |
| NFR-08 后台暂停 | ✅ | 与阶段四 #5 一致：`visibilitychange` |
| NFR-03 包体积 | 🔶 | 引入 framer-motion 后主 JS gzip 约 **91KB**（仍 &lt; 200KB）；需发版前再复核 |

---

## 四、任务分解 Sprint / TASK（进展）

| 任务 | 进展 | 说明 |
|------|:----:|------|
| TASK-001 项目初始化 | ✅ | 与阶段一一致 |
| TASK-002 移动端适配 | 🔶 | viewport、`MobileFrame`、body `touch-action` 有；letterbox 方案文档级未单独验收 |
| TASK-003 Canvas 层 | ✅ | `GameCanvas`、`useGameLoop`、坐标与 DPR |
| TASK-004 物理引擎 | ✅ | `engine/physics.ts`、`collision.ts`；`usePhysics` 仍为占位（逻辑在 Canvas） |
| TASK-005 挡板控制 | ✅ | `usePaddle.ts` + Canvas 事件 |
| TASK-006 砖块系统 | ✅ | 配置生成 + Canvas 绘制 + 碰撞 |
| TASK-007 计分与生命 | ✅ | `gameStore` + HUD |
| TASK-008～012（Sprint 2） | 🔶 | 粒子/音效/过渡/弹窗等已部分落地；加载页、独立结算页、完整 Loading 等仍未接线 |
| TASK-013 及 Sprint 3 | ⬜ | 未开展 |

---

## 五、统计汇总（便于汇报）

### 5.1 监管 Review（仅 §二 表格 37 行，不含追加行）

| 符号 | 项数 | 占 37 项 |
|:----:|-----:|---------:|
| ✅ | 28 | ≈ 76% |
| 🔶 | 4 | ≈ 11% |
| ⬜ | 0 | 0% |
| ⚠️ | 5 | ≈ 14% |

### 5.2 FR（§三 表内条数汇总）

| 符号 | 条数（约） |
|:----:|----------:|
| ✅ | 16 |
| 🔶 | 5 |
| ⬜ | 0 |

### 5.3 口头汇报模板（复制即用）

- **已完成**：工程初始化 + 核心打砖 + **阶段三主线**：粒子、拖尾、星空（简版）、Web Audio 音效、菜单↔游戏过渡、暂停与后台暂停、通关弹窗（得分+星级+下一关）。  
- **进行中 / 部分**：主菜单缺排行榜/设置、加载页未进流程、关卡未满 5 关、游戏结束仍为页内遮罩、星空无流星、Git 待确认。  
- **未开始**：排行榜、设置页 UI、部署与多数真机/性能专项测试。

---

## 六、建议下一步（按优先级）

1. ⬜ 加载页进主流程（FR-01）  
2. 🔶 独立 `GameOverPage` 路由（FR-09 余量）  
3. ⬜ 排行榜 + 设置页接线（FR-10、FR-11）  
4. 🔶 星空增强（流星 / 视差，对齐 Sprint2 TASK-012）  
5. ⬜ 补满 5+ 关卡（FR-04）  
6. ⚠️ 阶段二～四真机 Review，并在 `任务计划与需求清单.md` 打勾  

---

## 七、与需求清单文档的关系

- **打勾验收**：仍以 `docs/任务计划与需求清单.md` 内表格为准（负责人签字）。  
- **查进度**：以**本文件**各表「进展」列为准，每周或每迭代更新一次即可。  
