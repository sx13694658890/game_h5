import type { BallState, PaddleState, Vec2 } from '../types/game'

/** 挡板反射（阶段二实现完整偏转算法） */
export function reflectOffPaddle(_ball: BallState, _paddle: PaddleState): Vec2 {
  void _ball
  void _paddle
  return { x: 0, y: -1 }
}
